en:
  discourse_threads:
    errors:
      missing_topic_managers: "Topic manager users not found: %{usernames}"
      required_site_setting: "%{setting} is required while Threads is enabled."
      too_many_topic_managers: "You can add up to %{count} topic co-managers."
  site_settings:
    discourse_threads_enabled: "Enable per-topic thread mode controls and topic managers for nested replies."
    nested_replies_enabled: "Enable nested replies. This setting is required and automatically managed by Threads while the plugin is enabled."
    discourse_threads_default_thread_mode_enabled: "Show topics in thread mode by default for users who have not chosen a per-topic preference."
